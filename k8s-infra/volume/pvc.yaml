apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-connect-pvc
  namespace: kafka-connect
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
