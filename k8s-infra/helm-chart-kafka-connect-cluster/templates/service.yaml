apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-kafka-connect-cluster.fullname" . }}
  labels:
    {{- include "helm-chart-kafka-connect-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-kafka-connect-cluster.selectorLabels" . | nindent 4 }}
